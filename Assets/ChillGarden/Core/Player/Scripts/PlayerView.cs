using Core.Interactable;
using UnityEngine;

namespace Core.Player
{
    public class PlayerView : MonoBehaviour, IInteractableController
    {
        public void InteractWith(IInteractable interactable)
        {
            
        }

        public void Move(Vector3 position)
        { 
            
        }
    }
}
